<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ecommerce - Acesso Restrito</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light py-5">

    <div class="container">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Usuários</h1>
        </div>

        <div class="card shadow-sm mb-4">
            <div class="card-body">
                <h2 class="h5">Lista de usuários cadastrados:</h2>
                <ul class="list-group list-group-flush">
                    {{#each usuarios}}
                        <li class="list-group-item">{{this.user}}</li>
                    {{/each}}
                </ul>
            </div>
        </div>

        <div class="card shadow-sm">
            <div class="card-body">
                <h2 class="h5 mb-3">Cadastrar novo usuário:</h2>
                <form action="/usuario_post" method="post">
                    <div class="mb-3">
                        <label for="usuario" class="form-label">Nome do novo usuário</label>
                        <input type="text" name="user" class="form-control" required>
                    </div>

                    <div class="mb-3">
                        <label for="password" class="form-label" placeholder="Digite o nome do novo usuário">Senha</label>
                        <input type="password" name="password" class="form-control" required placeholder="Digite a senha do novo usuário">
                    </div>

                    <button type="submit" class="btn btn-success">Cadastrar</button>
                </form>
            </div>
        </div>

    <a href="/logout" class="btn btn-outline-danger mt-4">Sair</a>
    </div>

</body>
</html>
